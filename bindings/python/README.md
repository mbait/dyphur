# bindings/python

Python bindings via [nanobind](https://github.com/wjakob/nanobind). Not yet wired in — `bindings/python/` is excluded from the top-level `CMakeLists.txt` until the C++ public API stabilizes (Phase 2 at earliest).
